import {createStyles} from '@material-ui/core'

export const styles = () => createStyles({
    root: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center'
    },
    icon: {
        margin: '0px 10px 0px 0px'
    }
})
